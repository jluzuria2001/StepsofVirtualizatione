echoping -4 -v localhost

# Installing the [xinetd] packet, then enabling in the file [ /etc/xinetd.d/echo ]

# If the port [7] is close, look with the [ nmap ] command (as a root): 

nmap -sV -Pn -p 7 localhost
